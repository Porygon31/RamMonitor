<UserControl x:Class="RamMonitor.App.Views.AlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:RamMonitor.App.Converters"
             Background="{StaticResource WindowBackgroundBrush}">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Alerts" Style="{StaticResource HeaderText}" />
            <TextBlock Text="Live alerts (highest first). Load history to query the persisted store."
                       Style="{StaticResource SubHeaderText}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="📥  Load last 24h" Style="{StaticResource AccentButton}"
                    Command="{Binding LoadHistoryCommand}" Margin="0,0,8,0" />
            <Button Content="🗑  Clear view" Command="{Binding ClearCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Alerts}" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Width="160"
                                    Binding="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" />
                <DataGridTextColumn Header="Severity" Width="100" Binding="{Binding SeverityLabel}">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Severity}" Value="Critical">
                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Title" Width="220" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Message" Width="*" Binding="{Binding Message}" />
                <DataGridTextColumn Header="Source" Width="180" Binding="{Binding Source}" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Margin="0,8,0,0"
                   Foreground="{StaticResource ForegroundMutedBrush}"
                   Text="{Binding StatusMessage}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource BoolToVis}}" />
    </Grid>
</UserControl>
