<UserControl x:Class="RamMonitor.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:conv="clr-namespace:RamMonitor.App.Converters"
             Background="{StaticResource WindowBackgroundBrush}">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
        <conv:InverseBoolConverter x:Key="InvBool" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="History" Style="{StaticResource HeaderText}" />
            <TextBlock Text="Query persisted snapshots from SQLite. Filter a process by PID for its timeline."
                       Style="{StaticResource SubHeaderText}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" Width="140"
                      ItemsSource="{Binding TimeRanges}"
                      SelectedItem="{Binding SelectedRange}"
                      Margin="0,0,8,0" />

            <TextBox Grid.Column="1" Width="140"
                     Text="{Binding ProcessFilterPid, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Optional: PID to overlay process working set"
                     Margin="0,0,8,0" />

            <Button Grid.Column="2" Content="🔄  Refresh" Style="{StaticResource AccentButton}"
                    Command="{Binding RefreshCommand}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InvBool}}"
                    Margin="0,0,8,0" />

            <ProgressBar Grid.Column="3" Width="120" IsIndeterminate="True"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                         VerticalAlignment="Center" />
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="12">
            <lvc:CartesianChart Series="{Binding Series}"
                                XAxes="{Binding XAxes}"
                                YAxes="{Binding YAxes}"
                                LegendPosition="Top" />
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0">
            <TextBlock Text="Export current snapshot:" VerticalAlignment="Center" Margin="0,0,12,0"
                       Foreground="{StaticResource ForegroundMutedBrush}" />
            <Button Content="CSV" Command="{Binding ExportSnapshotCommand}" CommandParameter="csv"
                    Margin="0,0,8,0" />
            <Button Content="JSON" Command="{Binding ExportSnapshotCommand}" CommandParameter="json"
                    Margin="0,0,8,0" />
            <Button Content="PNG" Command="{Binding ExportSnapshotCommand}" CommandParameter="png"
                    Margin="0,0,8,0" />
            <Button Content="HTML Report" Style="{StaticResource AccentButton}"
                    Command="{Binding ExportSnapshotCommand}" CommandParameter="html" />
        </StackPanel>

        <TextBlock Grid.Row="4" Margin="0,8,0,0"
                   Foreground="{StaticResource ForegroundMutedBrush}"
                   Text="{Binding StatusMessage}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource BoolToVis}}" />
    </Grid>
</UserControl>
